# Environment Info (Phase 0 / Step 0.2 + Step 0.4)
# Captured at: 2026-02-14
# Python environment: /home/yangximing/envs/my_jupyter_env

## python --version
Python 3.10.12

## pip show tensorflow | grep Version
Version: 2.20.0

## pip show torch | grep Version
Version: 2.9.1

## nvcc --version
/bin/bash: line 1: nvcc: command not found

## python -c "import torch; print(torch.backends.cudnn.version())"
91002

## nvidia-smi | head -3
Failed to initialize NVML: Unknown Error

## TensorFlow GPU check
Command:
python -c "import tensorflow as tf; print(tf.config.list_physical_devices('GPU'))"
Output:
[]

## PyTorch GPU check
Command:
python -c "import torch; print(torch.cuda.is_available(), torch.cuda.device_count(), torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'N/A')"
Output:
False 0 N/A
